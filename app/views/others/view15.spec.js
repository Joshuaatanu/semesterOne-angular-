'use strict';

describe('myApp.view15 module', function() {

  beforeEach(module('myApp.view15'));

  describe('view15 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view15Ctrl = $controller('View15Ctrl');
      expect(view15Ctrl).toBeDefined();
    }));

  });
});